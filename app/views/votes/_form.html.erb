<%= form_with(model: [@election, vote], local: true) do |form| %>
  <% if vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
        <% vote.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :m %>
    <%= form.text_field :m %>
  </div>

  <div class="field">
    <%= form.label :r %>
    <%= form.text_field :r %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
