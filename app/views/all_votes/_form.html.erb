<%= form_with(model: all_vote, local: true) do |form| %>
  <% if all_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(all_vote.errors.count, "error") %> prohibited this all_vote from being saved:</h2>

      <ul>
        <% all_vote.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
